---
description: CgsAgent 代码变更时自动同步架构文档和 Skill 文档
globs:
  - src/backend/cds/ext/tencent/cgsagent/**/*.kt
  - src/console/backend_src/internal/monitoring/**/*.go
  - src/console/frontend_src/src/views/console/monitoring/**/*.vue
---

# CgsAgent 文档同步规则

> 本规则是 `workflow.mdc` 中「架构文档检查」的 CgsAgent 专属细化版。当本规则生效时，`workflow.mdc` 的架构文档检查项由本规则接管，避免重复。
> 本规则仅覆盖技术文档同步，需求文档、进展追踪等仍由 `workflow.mdc` 管理。

当修改匹配文件时，必须执行以下完整流程（前置 → 编码 → 同步），不可跳过任何阶段。

## 修改前：读取文档（必须）

**在修改匹配文件的任何代码之前**，先读取以下文档中与本次变更相关的章节：

1. `docs-harness/architecture/cgsagent.md` — 理解当前架构、核心类索引、数据结构定义、调用关系
2. `.cursor/skills/schedule-architecture/SKILL.md` — 理解全链路流程和设计决策
3. `.cursor/skills/schedule-architecture/reference.md` — 理解详细的类索引和实现细节

> **为什么**：这些文档是面向 AI 助手的编码参考，先读可以避免重复摸索代码结构，同时让你在编码过程中自然意识到哪些文档描述需要更新。

## 修改后：同步文档（必须）

确保文档与代码一致。**文档同步是任务完成的必要条件**——代码修改 + 调试通过 + 文档同步，三者全部完成才算任务结束。不能因为进入调试流程而遗忘文档更新。

## 需要同步的文档

| 文档 | 路径 | 更新场景 |
|------|------|----------|
| 架构文档 | `docs-harness/architecture/cgsagent.md` | 类新增/删除/重命名、方法签名变更、流程变更、配置项变更、中间件依赖变更 |
| Skill 文档 | `.cursor/skills/schedule-architecture/SKILL.md` | 架构层面的变更（新增模块、流程调整、协议变更） |
| Skill 参考 | `.cursor/skills/schedule-architecture/reference.md` | 类索引、方法详情、Redis Key、异常处理等实现细节变更 |

## 判断是否需要更新

完成代码修改后，逐项检查：

1. **新增/删除/重命名了类或文件？** → 更新架构文档「核心类索引」和「目录结构」
2. **修改了公开方法签名？** → 更新架构文档中引用该方法的所有位置
3. **新增了 Kafka Topic？** → 更新「中间件依赖 > Kafka」表格
4. **新增/修改了 Redis Key？** → 更新「中间件依赖 > Redis」表格
5. **新增了 API 端点？** → 更新「API 端点」表格
6. **修改了消息类型或协议？** → 更新「自定义二进制协议 > 消息类型」
7. **修改了配置项？** → 更新「Netty TCP 服务 > 服务端配置」或「部署配置」
8. **修改了异常处理策略？** → 更新「异常处理机制」
9. **修改了连接管理逻辑？** → 更新「连接管理」和「CDS 连接生命周期」
10. **修改了监控相关代码？** → 更新「监控」章节

## 更新要求

- 架构文档同时面向开发人员和 AI 编码参考，保留精确的类名、方法签名、文件路径
- 更新架构文档的「编码注意事项」，如果本次修改引入了新的需要注意的约定或陷阱
- 如果是纯重构（不改变行为），只需更新类索引和目录结构，不需要更新流程图
- 完成文档更新后，在回复中简要说明更新了哪些文档的哪些章节

## 任务完成标准

以下条件 **全部满足** 才算任务完成，缺一不可：

1. ✅ 代码修改完成
2. ✅ 功能验证/调试通过（如果涉及）
3. ✅ 文档同步完成（按上述 10 项清单逐项检查）
4. ✅ 在回复中说明了文档更新内容

> **常见陷阱**：代码改完后用户报 bug → 进入调试 → 修复后认为任务完成 → 遗忘文档更新。正确做法：**每次回复"任务完成"之前，先检查文档是否已同步**。
