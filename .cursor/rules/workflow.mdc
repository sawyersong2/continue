---
description: 开发工作流规范，定义文档结构、记忆系统和协作约定。在涉及需求文档、架构文档、进展追踪、复盘等场景时自动生效。
alwaysApply: true
---

# 开发工作流规范

## Git 分支与 MR 方向（强制）

- `develop` 只能通过开发分支发起 MR 合入，禁止为了解冲突或重新提交 MR，主动把 `develop` merge / pull 到当前开发分支。
- 当开发分支合入 `develop` 出现冲突时，应在 MR 平台或按团队约定处理冲突；不要通过“先把最新 `develop` 合回开发分支，再重新提交 MR”的方式解决。
- 如需同步主干变更，必须先向用户说明风险并取得明确确认；默认不执行 `git merge develop`、`git pull origin develop` 到开发分支。

## 文档即工作工具（最高优先级，所有模式强制执行）

`.cursor/rules/`、`.cursor/skills/`、`docs-harness/` 目录下的文档是 **AI 助手的工作工具和知识库**，不是任务的输出物。在 Agent、Plan、Ask **任何模式**下，只要用户的问题涉及项目代码、架构、模块、需求，**必须先查阅相关文档再回答或动手**。

### 强制查阅流程

**每次收到涉及代码的请求时**（包括但不限于：实现需求、修改代码、方案设计、代码审查、排查问题、回答架构问题），在思考和回复之前，执行以下查阅：

1. **`docs-harness/architecture/`** — 是否有涉及模块的架构文档？有则读取相关章节，理解模块职责、类索引、数据结构、调用关系
2. **`.cursor/skills/`** — 是否有匹配的 Skill 文档？有则读取，理解全链路流程和设计决策
3. **`docs-harness/requirements/`** — 是否有相关需求文档？有则读取 requirements.md 和 solution.md，理解需求背景和方案选型
4. **`docs-harness/progress.md`** — 需要了解当前项目状态时读取

### 查阅原则

- **先文档，后源码**：文档是对代码的结构化摘要，先读文档再定向读源码，比从源码逐个文件摸索效率高 10 倍
- **不确定就查**：不确定某个模块的架构或约定时，先查文档再回答，不要凭记忆或猜测
- **文档不存在不代表可以跳过查阅**：如果查了发现没有相关文档，说明这是文档缺口，编码完成后应补建

### 违规示例（绝对不允许）

- 用户说"帮我给 CgsAgent 加个功能"→ 直接 grep 源码开始改 ❌ → 应先读 `docs-harness/architecture/cgsagent.md` 和 `.cursor/skills/schedule-architecture/` ✅
- 用户说"这个模块怎么工作的"→ 凭上下文记忆回答 ❌ → 应先读架构文档再回答 ✅
- 用户说"帮我做个方案设计"→ 直接出方案 ❌ → 应先读已有架构文档和需求文档，基于现有设计出方案 ✅

### 自检（强制）：先声明读了什么，再动手

**触发**：任何涉及代码的任务，Plan 与 Agent 模式都适用。

**必须先输出**（在第一次 grep/Read 业务源码、派 subagent、出方案之前，向用户的回复正文里给一段）：

```
文档预查阅清单：
- skill：<读了哪些 .cursor/skills/*/SKILL.md 绝对路径；或 "无匹配"，需先用 Glob 确认>
- architecture：<docs-harness/architecture/*.md 路径；或 "无匹配"，需先 Glob>
- requirements：<docs-harness/requirements/NNN-*/*.md 路径；或 "无匹配"，需先 Glob>
```

**规则**：

- 「无匹配」是合法答案，但**必须**先用 Glob/Read 确认，不能凭印象。
- 主动决定跳读某项，必须在该行后给一句话原因（例：「无匹配，本次仅改文档索引」）。
- agent_skills 列表里出现的 skill 是「100% 应该读」，跳读需在原因里专门解释。
- 漏读且未声明清单 = 违规，停止当前动作回到查阅再重新声明。

## 文档目录结构

项目使用 `docs-harness/` 目录管理开发工作流文档：

```
docs-harness/
├── README.md                         # 文档总索引
├── progress.md                       # 当前快照（在做什么、下一步、卡点）
├── memory/                           # 工作日志（checkpoint 写入）
│   └── YYYY-MM-DD.md                # 每日存档
├── requirements/                     # 需求文档
│   ├── README.md                    # 需求索引（编号、状态、链接）
│   └── NNN-requirement-name/        # 三位数编号
│       ├── requirements.md          # 需求描述与分析
│       ├── solution.md              # 实现方案
│       └── task-items.md            # 任务拆解与进度
├── architecture/                     # 架构文档（持续迭代）
├── features/                         # 功能文档
└── postmortem/                       # 复盘记录
```

## 开发前置动作（必须遵守）

接到新需求或优化任务时，**无论是否有 plan 文件、todo 列表或明确的执行指令**，在编码前必须执行以下步骤：

1. **读取 `docs-harness/progress.md`**：了解项目当前状态和近期决策
2. **检查 `docs-harness/requirements/README.md`**：确认是否已有相关需求文档，避免重复
3. **读取相关架构文档或 Skill 文档**：
   - 检查 `docs-harness/architecture/` 中是否有涉及模块的架构文档，有则先读取相关章节
   - 检查 `.cursor/skills/` 中是否有匹配的 Skill 文档，有则先读取
   - **为什么**：架构文档和 Skill 文档是面向 AI 助手的编码参考，先读可以快速理解模块结构和调用关系，避免从源码逐个文件摸索
4. **判断是否需要创建需求文档**：
   - **追加优先于新建（强制）**：先在 `docs-harness/requirements/README.md` 中检索是否已有需求覆盖了本次改动的模块/能力。若已有需求覆盖，**必须**以追加形式更新原需求（在 solution.md 加修订小节、task-items.md 加任务项、必要时更新 requirements.md 范围），而**不是**新建编号
   - 仅当满足以下任一条件才新建需求编号：① 改动跨越完全不同的业务领域；② 目标完全独立、可独立提测/灰度；③ 原需求已归档且重启会引起严重歧义
   - 涉及多文件改动、有方案选型或设计决策且找不到可追加的原需求 → 使用 `new-requirement` skill 创建需求文档
   - 简单 bug 修复或单文件小改动 → 可跳过
   - **注意：plan 文件不能替代需求文档**。plan 是执行计划，requirements 是项目知识沉淀，两者必须独立存在
5. **如果存在相关需求文档**：先读取 requirements.md 和 solution.md，作为理解需求和方案的输入

如果需求在对话中逐步明确（如用户最初只说"帮我实现"，后续讨论才形成完整方案），编码完成后必须补建需求文档。

**重要**：收到"按 plan 实现"或"完成所有 todo"等执行指令时，第一个动作仍然是检查需求文档，而非直接编码。将"创建/更新需求文档"作为第一个 todo 执行。

## 需求文档规范

- 需求使用三位数编号，如 `001-cleanup-user-instance`
- 每个需求目录包含三个标准文件：requirements.md（需求描述）、solution.md（实现方案）、task-items.md（任务拆解）
- 新建需求时更新 `docs-harness/requirements/README.md` 索引
- 需求状态：规划中 → 开发中 → 测试中 → 已完成 / 已取消
- **TAPD（BKW）**：新建或变更需求状态时，按 `tapd-requirement-sync.mdc` 使用 TAPD MCP 在 workspace **69990282** 创建/更新需求单（标题前缀 **【云桌面后台】**），并在开发各阶段同步 TAPD 状态

### 追加 vs 新建判定（强制规则）

为避免需求碎片化、TAPD Story 与编号体系膨胀，**已有需求范围内的修订、补丁、后续优化，一律以「追加」形式更新原需求文档，而不是新建编号**。

- **判定依据**：本次改动涉及的模块/能力是否已在某个既有需求中实现过？相关代码主体是否在该需求中落地？
- **追加的标准位置**：
  - `solution.md`：在末尾加「修订记录」小节，写「YYYY-MM-DD：何处改了什么、为什么、影响范围」
  - `task-items.md`：补一条修订任务及其状态
  - `requirements.md`：仅当原需求范围确实需要扩展时才动；若只是诊断逻辑/阈值/分级修订，通常无需改 requirements.md
- **TAPD**：追加修订**不新建** Story，直接在原需求关联的 Story 上更新描述或留评论；状态保持原 Story 已有的状态（已完成的需求若做修订，可在评论中说明，不必重新打开）
- **唯一允许新建编号的场景**：① 改动跨越完全不同的业务领域；② 目标完全独立、可独立提测/灰度发布；③ 原需求已归档且重启会引起严重歧义。三者满足任一才新建
- **示例**：`011-schedule-ai-analysis` 已包含 `ScheduleRuleEngine`，则该引擎的诊断 finding 拆分应作为 011 的修订记录追加，**不**新建独立编号

## progress.md 规范

`docs-harness/progress.md` 是项目当前状态的快照，永远只反映最新状态（非追加式日志）：
- 当前进行中的工作
- 待办事项
- 最近完成的工作
- 卡点和待决策事项
- 近期决策记录

## 工作日志规范

`docs-harness/memory/YYYY-MM-DD.md` 按天记录工作内容：
- 完成事项（含关键代码变更摘要）
- 决策记录（做了什么决策、为什么）
- 进行中的工作状态
- 遇到的问题和解决方案
- 同一天多次 checkpoint 追加写入，不覆盖已有内容

## 架构文档规范

`docs-harness/architecture/` 记录系统架构设计：
- 使用 Mermaid 图表描述流程和架构
- 重大架构变更需记录变更原因和影响范围
- 架构文档应与代码保持同步

## 上下文保持约定

- 完成阶段性工作后，主动提醒用户是否需要 checkpoint 存档
- 对话涉及重要决策时，建议将决策记录到工作日志
- 新对话开始时，如果用户提到恢复上下文或继续工作，引导使用 recap skill
- 踩坑修复后，建议进行 postmortem 复盘

## 完成后检查清单（checkpoint 时必须核对）

完成阶段性工作执行 checkpoint 时，除了写入工作日志和更新 progress.md，还必须核对以下项目：

1. **需求文档**：本次工作是否涉及新需求？如果是，`docs-harness/requirements/` 中是否已有对应文档？没有则**立即创建**（而非仅提醒）
2. **需求状态**：已有需求文档的状态是否需要更新（如 开发中 → 已完成）？`task-items.md` 中任务状态是否同步？若已建 TAPD Story，是否已按 `tapd-requirement-sync.mdc` 调用 MCP 同步 TAPD 状态？
3. **架构文档**：本次工作是否涉及架构变更（新模块、新中间件依赖、新的跨服务调用）？如果是，检查 `docs-harness/architecture/` 或相关 skill 文档是否需要更新。**注意**：如果变更的模块已有专属文档同步 Rule（如 `cgsagent-doc-sync.mdc`），以专属 Rule 为准，此处不重复检查
4. **功能文档**：本次工作是否新增了用户可见的功能（新页面、新 API、新监控面板等）？如果是，在 `docs-harness/features/` 中创建功能文档（含功能概述、数据流、API、配置、注意事项），并更新 `docs-harness/features/README.md` 索引
5. **需求索引**：`docs-harness/requirements/README.md` 中状态是否与实际一致
